# 비밀·고객 데이터·저작물 (커밋 금지)
.env
data/raw/
state/
evidence/
ref/**
*.hwp
*.hwpx

# 파이썬
__pycache__/
*.pyc
venv/
.venv/
build/
dist/
*.egg-info/

# 생성물 (코드에서 재생성 가능)
cb_valuation/step1_curve/graph/edges_export.json
cb_valuation/step1_curve/graph/graph.mmd

# 도구
.claude/settings.local.json

# 사용자 결정 2026-09-07: tests/fixtures/*.csv|json 과 reference/xl_*.txt(참조 모형 금리표·검토자 패키지 값)는
# 회사 내부 비공개 저장소 전제로 커밋한다. 공개/외부 공유 시 아래 두 줄의 주석을 해제한다.
# cb_valuation/step1_curve/tests/fixtures/*.csv
# cb_valuation/step1_curve/reference/xl_*.txt

# Office 잠금 파일
~$*

# PC 별 개인 설정(담당자 목록 등)
config/

# 화면 "엑셀 내려받기" 산출물(승인 전 임시 xlsx)
exports/

# 정적 배포본(tools/build_web.py 가 재생성; GitHub Actions 가 빌드해 Pages 에 올림)
web/
